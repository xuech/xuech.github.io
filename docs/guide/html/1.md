---
title: Html-浏览器是如何渲染页面的？
tags:
 - html
categories: 
 - frontEnd
date: 2019-10-11
---

## 浏览器页面渲染流程
### webkit渲染引擎流程
[1](./imgs/1.png)
### 关键渲染路径
>浏览器从最初接收请求来的html、css、js等资源，然后解析、构建树、渲染布局、绘制，最后呈现给客户能看到的界面这整个过程

浏览器的渲染过程主要包括以下几步
- 构建对象模型：解析html生成`DOM Tree`，解析css生成`CSSOM Tree`
- 构建渲染树：将`DOM Tree`与`CSSOM Tree`合并生成`Render Tree`
- 遍历渲染树开始布局（layout），计算每个节点的位置大小信息
- 将渲染树每个节点绘制（painting）到屏幕

注意：
- 当浏览器遇到一个script标签时，DOMTree的构建将被暂停，直至脚本执行完毕
- js可以查询和修改DOMTree与CSSOMTree
- 直至CSSOM构建完毕，js才会执行
- 脚本在文档中的位置很重要，css资源先于js资源。
  
## js和css对页面渲染的影响
css加载不会阻塞DOM树的解析,但会阻塞DOM树的渲染，也会阻塞后面js语句的执行
js会让CSSOM的构建阻塞DOM的构建

### defer与async
- defer属性和async属性都能防止脚本下载阻塞页面渲染
- 两者都只适用于外联脚本
- 多个声明了defer的脚本则按顺序下载、执行，后者无序
- defer脚本会在DOMContentLoaded和load事件之前执行，后者只保证在load事件之前执行

## 回流和重绘
在操作 DOM 时涉及到元素、样式的修改，就会引起渲染引擎重新计算样式生成 CSSOM 树，同时还有可能触发对元素的重新排布（简称“重排”）和重新绘制（简称“重绘”）。
引起重排的操作：
- 修改元素边距、大小 
- 添加、删除元素
- 改变窗口大小

重绘：
- 设置背景图片
- 修改字体颜色
- 改变 `visibility` 属性值

重排会导致重绘
在循环外操作元素


## 几条关于优化渲染效率的建议
- 合法地去书写 html 和 css ，且不要忘了文档编码类型。
- 样式文件应当在 head 标签中，而脚本文件在 body 结束前，这样可以防止阻塞的方式。
- 简化并优化css选择器，尽量将嵌套层减少到最小。
- 尽量减少在js中进行DOM操作。
- 修改元素样式时，更改其class属性是性能最高的方法。
- 尽量用 `transform` 来做形变和位移
  
## 总结：
- CSSOMTree需要等到完全构建后才可以被使用，因为后面的属性可能会覆盖掉前面的设置
- css被视为阻塞渲染的资源，应放到代码的头部尽快加载
- 同步的js会暂停DOMTree的构建，应放到代码的尾部最后加载，或者使用async/defer属性异步加载js
- 重排和重绘会给浏览器渲染线程造成很大的负担，尽量减少重排和重绘的触发次数

[参考:知乎](https://zhuanlan.zhihu.com/p/74792085)
[参考:blog](https://srtian96.gitee.io/blog/2018/06/01/%E6%B5%8F%E8%A7%88%E5%99%A8%E6%B8%B2%E6%9F%93%E5%8E%9F%E7%90%86/)