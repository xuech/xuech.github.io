---
title: vue-基础知识
tags:
 - vues
categories: 
 - frontEnd
date: 2019-12-28
---

### 列表渲染
- 如何遍历对象
- key的重要性
- v-for不能和v-if不能一起用

### 事件
- event 参数，自定义参数
- 事件修饰符，按键修饰符
- 【观察】事件被绑定到哪里


### 组件
- 父子组件的生命周期及调用顺序
- 父子组件如何通讯、如何用自定义事件进行vue组件通讯
- 函数式组件



### 路由
- 

### 高级特性
- 自定义v-model
- $nextTick
- slot
  - 作用域插槽
  - 具名插槽
- 动态、异步组件
- keep-alive
- mixin

## 优化
- 如果一个组件是纯展示且不需要有响应式数据状态的处理的可以使用函数式组件(无状态组件)替换， `functional: true`
  - 无需维护响应数据
  - 无钩子函数
  - 没有`instance`实例,组件内部没有办法像传统组件一样通过this来访问组件属性

- 合理使用`v-if`与`v-show`
  - `v-if` 是惰性的，当条件是`false`的时候不渲染dom，而`v-show`无论如何都渲染dom。
  - `v-if `切换开销大，`v-show`初始渲染开销大。所以对于频繁切换的组件，建议使用`v-show`。

- mixin



